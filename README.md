# 📄 get_next_line

**get_next_line** is a function that reads a file or input line by line.  
It was developed to practice low-level file handling, memory management, and buffer logic in C.

## 📦 Usage

``` bash
git clone git@github.com:iriscodinggit/get_next_line.git
cd get_next_line
make
```
